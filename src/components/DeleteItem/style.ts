import styled from "styled-components";

export const Container = styled.div`
  font-size: 20px;
  transition: 0.2s;
  border-radius: 50%;
  padding: 3px;
  margin: 0px 3px;

  &:hover {
    cursor: pointer;
    background-color: #ffc300;
  }
`;
